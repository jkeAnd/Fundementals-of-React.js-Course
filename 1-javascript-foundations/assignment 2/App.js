const movie = {title: "Inception", year: 2010, director: "Nolan"};

const {title, year, director} = movie; // creating variables that match the property names 

const message = `The movie "${title}" was released in ${year}`; // Using template literals to create a string using variables

console.log(message); // I noticed that the title of the movie is not in "".



